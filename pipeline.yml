env:
  BUILDKITE_ARTIFACT_UPLOAD_DESTINATION: 'rt://https://personaluse4.jfrog.io/artifactory/test123-terraform'
  BUILDKITE_ARTIFACTORY_URL: 'https://personaluse4.jfrog.io/artifactory'
  BUILDKITE_ARTIFACTORY_USER: 'admin'
  BUILDKITE_ARTIFACTORY_PASSWORD: 'password'

steps:
- command: echo "hello world" > test.txt
  plugins:
  - artifacts#v1.9.3:
      upload: "test.txt"
